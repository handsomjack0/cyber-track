
name = "cybertrack"
compatibility_date = "2024-02-08"
pages_build_output_dir = "dist"

# D1 数据库绑定
# 【重要】请将 database_id 替换为您自己在 Cloudflare D1 创建的数据库 ID
[[d1_databases]]
binding = "DB"
database_name = "cybertrack-db"
database_id = "3048df8f-b102-450b-9f35-351a6dac9587"

# 环境变量配置
# 注意：API_SECRET 和 TELEGRAM_BOT_TOKEN 等敏感信息不应在此处硬编码
# 请务必在 Cloudflare Pages 后台 -> Settings -> Environment variables 中添加
[vars]
# 此处留空，完全依赖后台配置
